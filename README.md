# Proxima Mumbai

A standard Next.js site that runs locally without authentication or hosted-platform dependencies.

## Run locally

Requires Node.js 20.9 or newer.

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Other commands

```bash
npm run build
npm run start
npm run lint
```
